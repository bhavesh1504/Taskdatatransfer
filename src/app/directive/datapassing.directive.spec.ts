import { DatapassingDirective } from './datapassing.directive';

describe('DatapassingDirective', () => {
  it('should create an instance', () => {
    const directive = new DatapassingDirective();
    expect(directive).toBeTruthy();
  });
});
